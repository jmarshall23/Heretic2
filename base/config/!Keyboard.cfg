bind    1               "use staff"
bind    2               "use fball"
bind    3               "use hell"
bind    4               "use array"
bind    5               "use rain"
bind    6               "use fwall"
bind    7               "use phoen"
bind    8               "use sphere"
bind    9               "use mace"

bind    q               "use *powerup"
bind    w               "use *ring"
bind    e               "use *meteor"
bind    r               "use *morph"
bind    t               "use *tele"
bind	  y			"use *lshield"
bind    g               "use *tornado"

bind s weapprev
bind d weapnext

bind shift +speed
bind ctrl +attack
bind alt +defend
bind space +action

bind_double shift +creep

bind uparrow +forward
bind downarrow +back
bind leftarrow +left
bind rightarrow +right
bind kp_up +forward
bind kp_down +back
bind kp_left +left
bind kp_right +right

bind_double leftarrow +roll_left
bind_double rightarrow +roll_right
bind_double kp_left +roll_left
bind_double kp_right +roll_right

bind kp_end +moveleft
bind kp_pgdn +moveright
bind kp_home weapprev
bind kp_pgup weapnext

bind z +movedown
bind a +moveup

bind PAUSE   "pause"
bind ESCAPE  "togglemenu"
bind ~       "toggleconsole"
bind `       "toggleconsole"
bind F1      "cmd help"
bind t       "messagemode"
bind +       "sizeup"
bind =       "sizeup"
bind -       "sizedown"
bind INS     +klook
