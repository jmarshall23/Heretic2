// Put all aliases and post setup commands here
// Or in another user.cfg file in your userdir

// This command is also done in the default.cfg, but does not appear 
// on the console because the system is not yet fully initialized.
// Leave this to view system ID upon startup, or replace with any commands you wish.
cpuid